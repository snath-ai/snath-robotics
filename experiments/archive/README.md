# Archive

Superseded result files kept for provenance, not for reproduction.

- `results_pre_iceworld_draft.json` — from an early PERSIST protocol draft
  (6-phase, no Force zone) that predates `experiments/persist/ice_world.py`.
  Phases in this file "resolve" in ~8 steps; the published protocol
  (`experiments/persist/run_experiment.py`, 7 phases) escalates in every
  phase instead, per Table 2 of the PERSIST paper. Superseded by
  `experiments/persist/persist_results_*.json`.
